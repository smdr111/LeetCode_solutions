import pandas as pd

def article_views(views: pd.DataFrame) -> pd.DataFrame:
    views.drop_duplicates(inplace=True)
    df=views[views['viewer_id']==views['author_id']]
    df=df.rename(columns={'viewer_id':'id'}).sort_values(by='id')
    return df[['id']].drop_duplicates()
    
