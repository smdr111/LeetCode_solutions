import pandas as pd

def rearrange_products_table(products: pd.DataFrame) -> pd.DataFrame:
    products=products.melt(id_vars='product_id',value_vars=products.columns[1:],var_name='store',value_name='price')
    return products[products['price'].notnull()]
    
