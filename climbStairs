class Solution:
    def climbStairs(self, n: int) -> int:
        if n==1 or n==2:
            return n
        x=list(range(1,n+1))
        for i in range(2,len(x)):
            x[i]=x[i-1]+x[i-2]
        return x[n-1]
        
