class Solution:
    def shuffle(self, nums: List[int], n: int) -> List[int]:
        x = nums[:len(nums)//2]
        y = nums[len(nums)//2:]
        result = []
        for i in range(len(nums)//2):
            result.append(x[i])
            result.append(y[i])
        return result
